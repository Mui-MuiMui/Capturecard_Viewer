# 変更履歴

このファイルの書式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に従い、バージョン番号は [セマンティック バージョニング](https://semver.org/lang/ja/) に従う。

## [未リリース]

### 追加

- 設定画面のオーディオ設定に「音声バッファ」を追加した。入力から出力へ受け渡すリングバッファの長さを 20〜200ms のスライダーで選べる。小さいほど遅延が減るが、プチプチというノイズ（アンダーラン）が出やすくなる。最適な値は PC や機器によって違うため、これまで 50ms に固定していたものを選べるようにした。**既定は従来と同じ 50ms** なので、設定を引き継いだ場合の音の出かたは変わらない。変更が効くのは「適用」か「OK」を押したときで、そのとき音声だけが開き直る（映像は途切れない）。プリセットにも含まれる
- `GUARDRAIL.md` — してはいけないこと / 必ずすることだけを集めた一覧。理由は書かず、参照先だけを添えてある
- `docs/design/` — 設計判断の理由と経緯。ワーカースレッド、再接続、映像・音声、設定、ホットキーなどテーマ別に分けた 13 ファイル
- `AGENTS.md` — Claude Code 以外の AI エージェント向けの入口。`CLAUDE.md` / `GUARDRAIL.md` / `CONTRIBUTING.md` / `docs/design/` / `docs/ARCHITECTURE.md` と検証手順への道案内だけを持つ
- 設定画面の「接続状態」タブの音声の欄に、クロックドリフト補正のリサンプル比とリングバッファのバッファ水位を表示するようにした。入出力の形が揃っていて変換が要らない場合は「変換なし」と出す

### 修正

- 最小化している間、ホットキーのうち画面が要らないもの（デバイス再接続・音量を上げる / 下げる・ミュート切替）がその場で効くようにした。これまではウィンドウを元に戻すまで実行が持ち越され、音量を下げたつもりが鳴りっぱなし、といったことが起きていた。画面が要るもの（スクリーンショット・フルスクリーン切替・最前面表示の切替）は従来どおり復帰したときに実行するが、**最小化中に何度押したかをまとめて実行しない**ようにした。フルスクリーンなどの切り替えは押した回数が偶数なら実行せず（2 回押して元に戻したのと同じ扱い）、スクリーンショットは何回押しても 1 枚だけになる

## [1.1.0] - 2026-09-21

1.0.6 以降の全変更をまとめた版。ミュート・プリセット・クリップボード出力・ボーダーレス表示・映像調整などの機能追加と、デバイスの切断復帰・設定画面・音声まわりの多数の修正を含む。設定ファイルは旧形式のまま読める（ホットキーは新しい `[hotkeys]` 節へ自動で移行する）。

### 追加

- ビデオ・オーディオ設定の組み合わせに名前を付けて保存できるようにした（プリセット）。複数のキャプチャボードを使い分ける場合や、同じボードで「低遅延優先 (720p60)」「画質優先 (1080p30)」を行き来する場合に使える。管理は設定画面の「その他」タブ（一覧からの読み込み・上書き保存・削除と、現在の設定を新しいプリセットとして保存）、切り替えは右クリックメニューの「プリセット」から行う。**プリセットに入るのはデバイス設定タブのビデオとオーディオだけ**（デバイス・フォーマット・解像度・FPS・色空間・色レンジ・映像調整と、入出力デバイス・サンプリングレート・チャンネル数・音声パススルー）。スクリーンショットの設定・ホットキー・ウィンドウまわりの設定は含まない。デバイスの自動再接続も含まないので、右クリックメニューで切り替えた状態が残る。名前は重複できない。追加・上書き・削除・読み込みは他の項目と同じく編集中の設定に対して行うので、「適用」か「OK」を押すまで反映されず「キャンセル」で取り消せる。プリセットを読み込んだあとに設定を手で変えると「名前（変更あり）」表示になり、右クリックメニューのチェックも外れる。切り替えると画面下部に「プリセット: 名前」が 1.5 秒表示される。切り替え前と同じ内容ならデバイスの開き直しは起きない。プリセットが 1 つも無いあいだ、右クリックメニューに「プリセット」は現れない。書き出した設定ファイルにはプリセットも含まれ、読み込みでも入る。**「設定を初期化...」ではプリセットは消えない**（作り溜めたものを捨てる操作ではないため）
- 日本語フォントのフォールバックを改善した。これまで `meiryo.ttc` 固定で、Meiryo が入っていない環境（Windows の言語パックを最小構成にした場合など）では日本語が豆腐（□）になっていた。Meiryo → Yu Gothic UI（Medium / Regular）→ Yu Gothic → MS Gothic → BIZ UDGothic の順に候補を探し、最初に見つかったものを使う。`%WINDIR%\Fonts` に加えてユーザー単位でインストールされたフォント（`%LOCALAPPDATA%\Microsoft\Windows\Fonts`）も見る。どれも見つからない場合は egui の既定フォントのまま起動する（豆腐にはなるが落ちない）
- 設定画面に「その他」タブを追加し、設定の書き出し・読み込み・初期化をできるようにした。PC を移るとき、設定をバックアップしたいとき、不具合を報告するときに使える。書き出すのは**動作中の設定**で、既定のファイル名は `capturecard_viewer-settings-YYYYMMDD.toml`。読み込みと初期化は編集中の設定を書き換えるだけなので、「適用」か「OK」を押すまで反映されず、「キャンセル」で破棄できる。**ウィンドウの位置とサイズは読み込まない**（画面構成の違う PC で書き出したファイルを読んでもウィンドウが画面外へ出ない）。初期化も位置とサイズは保つ。右クリックメニューで切り替える項目のうち、最前面表示・画面ドラッグ移動・情報表示・ミュート・ボーダーレス表示も、設定画面から変更できないため読み込みの対象外。初期化は押し間違い防止のため確認を 1 段挟む。解釈できない値が書かれたファイルは、その項目だけが既定値になり残りは読み込まれる。ファイルとして壊れている場合は何も変えずに理由を表示する。これまで設定を初期化するには `%AppData%` のフォルダを手で削除するしかなかった
- 設定画面のビデオ設定に「映像調整」（明るさ / コントラスト / 彩度）を追加した。いずれも -100〜100 のスライダーで 0 が無調整、「リセット」で 3 つとも戻る。色空間と色レンジを正しく合わせてもなお映像が暗い・色が薄い、といった機種ごとのクセを埋めるためのもの。調整は YUY2 → RGB の変換係数に畳み込むので画素ごとの処理は増えず（増えるのは 1 フレームにつき数回の係数の計算だけ）、変更した次のフレームから反映される（デバイスの開き直しは起きない）
- 設定画面のビデオ設定に「色空間」（自動 / BT.601 / BT.709）と「色レンジ」（リミテッド / フル）を追加した。色空間はこれまで解像度からの推定しかできず、SD で BT.709、HD で BT.601 を出すデバイスでは色がずれていた。色レンジはリミテッド（Y 16〜235）専用だったため、フルレンジ（Y 0〜255）で出すデバイスでは黒が潰れ白が飛んでいた。どちらも変更した次のフレームから反映され、デバイスの開き直しは起きない
- スクリーンショットの保存形式を JPEG と PNG から選べるようにした。JPEG は品質を 1〜100 で指定できる（既定は 90）。PNG は可逆なので、ゲーム画面の文字や細い線ににじみが出ない。拡張子も選んだ形式に合わせて `.jpg` / `.png` になる
- スクリーンショットの出力先を「ファイルに保存」「クリップボードにコピー」「両方」から選べるようにした。クリップボードへは圧縮せずそのままの画が入るので、撮ってすぐ Discord やチャットへ貼り付けられる。「クリップボードにコピー」だけを選んだ場合はファイルを作らない。既定はこれまでどおりファイルへの保存で、設定ファイルをそのまま引き継げる
- `CONTRIBUTING.md` — 不具合報告の入口、ビルドと検証、ブランチとコミット、バージョン番号と `CHANGELOG.md` の扱い、各ドキュメントの置き場所
- `CLAUDE.md` — Claude Code 向けのリポジトリの手引き
- `docs/ARCHITECTURE.md` — 目指すアーキテクチャと現状との差分
- `docs/BUILD.md` — ビルド手順と必要なツール
- `docs/MANUAL-TEST.md` — 実機での手動テストチェックリスト
- `.claude/skills/` — 命名規則とテスト方針
- `.claude/commands/cv/` — 計画・実装・レビュー・PR 作成の各コマンド
- 稼働中にデバイスが消えたことを検出するようにした。映像が 3 秒途切れると、止まった画を残さずに表示を戻し、デバイスを開き直す。USB を挿し直せば操作せずに映像と音声が戻る。音声は入出力ストリームのエラーで検出する
- 映像が出ていないときの表示を「デバイスは掴めているが信号が来ていない」と「デバイスそのものが見えていない」で分けた。前者は入力機器の電源やケーブル、後者は USB の接続を確認する、と切り分けられる
- 右クリックメニューに「デバイスの自動再接続」を追加した。上記の開き直しを切れる。切っても、止まった画が残り続けることはない。オン / オフの状態は設定に保存される（既定はオン）
- グローバルホットキーをスクリーンショット以外の操作にも割り当てられるようにした。フルスクリーン切替・最前面表示の切替・デバイス再接続・音量の上げ下げの 5 つが対象。設定画面のホットキー欄がアクションごとの一覧になり、行ごとにキーを設定・解除できる。既定で割り当てられているのは従来どおりスクリーンショットの F5 だけで、他は未設定から始まる
- 同じキーを複数のアクションに割り当てたときに、設定画面で警告するようにした。適用した場合は一覧で上にあるアクションだけが有効になる
- ホットキーの登録に失敗した場合、画面に通知を出し、設定画面のホットキー一覧にも理由を表示するようにした。これまではログにしか残らず、設定したのに効かない理由が画面から分からなかった
- デバイスに接続できない理由を画面に出すようにした。これまでは何が起きたかがログにしか残らず、映像が出ない理由をユーザーが知る手段が無かった。新しい失敗は画面下部中央に数秒表示し、映像が出ていない間はプレースホルダーの 2 行目にも理由を添える。同じ理由が続く間は 60 秒おきまでに間引くので、再接続を試している間に表示が出続けることはない
- 設定画面に「接続状態」タブを追加した。映像と音声それぞれについて、実際に開いたデバイス名・解像度・フォーマット・サンプリングレート・チャンネル数と、直近のエラーとその発生時刻を確認できる。設定画面で選んだ値とデバイスが実際に受け付けた値は食い違うことがあるため、確定した側を出している
- スクリーンショットの保存に失敗したときも画面に出すようにした。保存は別スレッドで行っているため、これまでは保存先に書き込めなくてもログにしか残らず、ホットキーを押しても何も起きないように見えていた。映像が表示されていない状態で撮ろうとした場合も同様に出る
- 右クリックメニューに「情報表示」を追加した。オンにすると映像の左上に実効 FPS、フレーム間隔のばらつき、RGB 変換にかかった時間、解像度とフォーマットを重ねて表示する。オン / オフの状態は設定に保存される
- ミュートを追加した。音量を 0% にする代わりに一時的に消音できる。**音量の値はミュート中もそのまま残るので、解除すれば元の音量で鳴り始める。** 切り替えは映像上のミドルクリック、右クリックメニューの「ミュート」、ホットキーの「ミュート切替」（既定は未設定）のいずれでも行える。切り替えると画面下部に「ミュート」／「ミュート解除（音量: N%）」が 1.5 秒表示される。ミュート中にホイールや音量ホットキーで音量を変えると、ミュートは解除される（これらの操作中は画面にミュートの表示が無く、解除しないと原因が分からないため）。右クリックメニューのスライダーでは解除せず、音量バーを灰色で描いて「効いていない」ことだけを示す。状態は設定に保存される

- 右クリックメニューに「タイトルバーを隠す」を追加した。タイトルバーと枠を消して映像だけを出せる。デュアルモニタでサブウィンドウとして置くときに装飾が邪魔にならない。隠している間の移動は映像部分のドラッグ、サイズ変更はウィンドウ端 8px ほどのドラッグ（四隅では斜め）で行う。**閉じるボタンが無くなるので、右クリックメニューに「終了」を追加した**（Alt+F4 も従来どおり使える）。隠すときに「画面ドラッグ移動」がオフだと、ウィンドウを動かす手段が無くなるため自動でオンにし、その旨を画面下部に 2 秒表示する。隠している間は「画面ドラッグ移動」をオフにできない。フルスクリーン中は元から装飾がないため切り替えられない。状態は設定に保存される（既定はタイトルバーあり）
- 右クリックメニューに「ウィンドウサイズをリセット」を追加した。既定の 1280x720 に戻す。タイトルバーを隠した状態で小さくしすぎて端を掴めなくなったときの復帰手段として使える
- 設定画面に「ホットキー」タブを追加した。これまでホットキーの一覧はスクリーンショット設定タブの中にあったが、フルスクリーン切替や音量操作などスクリーンショット以外のアクションも並ぶため、タブ名と内容が合っていなかった

### 変更

- ウィンドウを最小化している間も、デバイスの自動再接続が動くようにした。これまでは最小化中に USB を抜き差ししても、ウィンドウを元に戻すまで繋ぎ直さなかった
- 右クリックメニューが画面からはみ出して押せなくなることがあったのを直した。項目が増えて縦に長くなり、低い解像度やウィンドウを画面下端へ寄せた状態では、下のほうの項目が画面外へ出ていた。切り替え系の項目を「表示」（アスペクト比を維持・最前面表示・情報表示・タイトルバーを隠す）と「ウィンドウ」（画面ドラッグ移動・ウィンドウサイズをリセット）のサブメニューへまとめ、直下に残すのは音量・ミュート・フルスクリーン表示・デバイス再接続・デバイスの自動再接続・詳細設定・終了にした。加えて、メニューが画面の外へ出る位置で開いたときは画面内へ寄せ、それでも入りきらない高さのときはスクロールできるようにした。各項目の動作は変えていない
- 映像が届いていないときの CPU 使用率を下げた。これまでは映像の有無にかかわらず常に 60fps で描き直しており、デバイスを繋いでいない状態でも CPU を使い続けていた。映像が届いている間は従来どおり 60fps、途切れてからは 4fps、最小化中は 1fps まで落とす。実測（1 コアを 100% とする値）で、映像が出ていない状態が 6.4% から 1.0% に下がった。映像表示中は変わらない。映像が戻ったときは間隔を待たずにその場で表示を再開するので、表示の遅れは増えない
- 設定画面のオーディオのサンプリングレートとチャンネル数の選択肢を、**入力デバイスと出力デバイスの両方が対応する値だけ**にした。これまでは固定の一覧を出しており、デバイスが対応しない値も選べて、選んだ値と実際に開く値が黙って食い違っていた。選択肢が 1 つしか無い場合（Windows の共有モードではチャンネル数が通常こうなる）は操作できない状態にして理由を出す。入出力で共通の値が無い場合は両方を並べて警告する。保存済みの値が選択肢に無い場合は、実際に使われる値を添えて警告する
- オーディオデバイスの対応設定の取得を別スレッドで行うようにした。WASAPI ではこの列挙に実測 300ms 前後かかり、これまでは起動時とオーディオデバイスの変更時にその間アプリ全体が固まっていた。音声を開く処理にも取得済みの一覧を渡すようにしたので、接続のたびに列挙し直すこともない
- 入力と出力で同じサンプリングレート・チャンネル数に揃えられる場合は、揃えて開くようにした。これまでは入出力それぞれが独立に「希望値に最も近い値」を選んでいたため、揃えられる組み合わせでも別々の値で開かれ、再生速度とピッチがずれることがあった
- ビデオデバイスの対応形式（フォーマット・解像度・フレームレート）の取得を別スレッドで行うようにした。これまでは設定画面でビデオデバイスを切り替えるたびに、取得が終わるまでアプリ全体が固まっていた
- 起動時に、保存済みのビデオデバイスの対応形式を先に取得するようにした。これまではデバイスを切り替えたときしか取得しておらず、起動後に設定画面を開いても解像度やフレームレートの選択肢が出なかった
- 対応形式の取得に失敗した場合、設定画面に理由と「再取得」ボタンを表示するようにした。これまでは失敗しても何も表示されず、選択肢が既定値のまま出ていた
- JPEG で保存するときの品質を 75 から 90 に上げた。これまでは image クレートの既定値をそのまま使っており、文字や細い線に圧縮の跡が見えていた。ファイルはおよそ 2 倍になるが、設定画面から元の値に戻せる
- 起動してからデバイスに接続するまでを短くした。これまでは起動後 2 秒待ってからデバイスを開き始めていた。実測で、接続が終わるまでが 2.8〜3.0 秒から 0.7〜0.9 秒になった。映像が実際に映るのはその後で、起動から 1.1 秒ほどかかる（キャプチャーボードが最初の 1 枚を出すまでの時間で、こちらからは短くできない）
- デバイスに接続できない場合、繋がるまで自動で再接続を試み続けるようにした。間隔は 0.2 秒から徐々に広げ、最大 5 秒間隔になる。キャプチャーボードをあとから挿した場合も、操作せずに接続される
- ホットキー入力ダイアログを、開いた瞬間からキー入力を受け付け、修飾キー以外のキーが押されたら自動で確定して閉じるようにした。これまでは「キャプチャ開始」→キー入力→「OK」の 3 段階だった。ダイアログを開いている間はグローバルホットキーの登録をすべて一時解除するようにしたので、既に他のアクションへ割り当て済みのキーも押した瞬間に実行されることなく再割当てできる。押したキーが修飾キーだけ・他のアクションに割り当て済み・他のアプリと競合していて登録できない、のいずれかで受け付けられない場合は、ダイアログを閉じずに理由を赤字で表示する。ボタンは「キャンセル」だけになり、「クリア」は一覧側の「クリア」に一本化した

### 変更（設定ファイル）

- `[screenshot]` に出力先の項目 `destination`（`"file"` / `"clipboard"` / `"both"`）を追加した。**これまでの設定ファイルはそのまま読める。** 項目が無い場合は `"file"` として扱う
- `[ui]` に `muted`（既定 `false`）を追加した。項目が欠けている既存の設定ファイルはそのまま読め、ミュート解除の状態で起動する
- `[ui]` に `borderless`（既定 `false`）を追加した。項目が欠けている既存の設定ファイルはそのまま読め、タイトルバーがある状態で起動する。`borderless = true` と `enable_drag_move = false` を手で書いた場合は、起動時に `enable_drag_move` を `true` へ直す（そのままではウィンドウを動かせないため）
- ホットキーの保存場所を `[screenshot]` の `hotkey` から、独立した `[hotkeys]` セクションへ移した。**これまでの設定ファイルはそのまま読める。** 起動時に `hotkey` の値がスクリーンショットへ移り、次に保存した時点で古い項目は消える。新しい版で一度保存したあとに古い版へ戻すと、ホットキーは既定の F5 に戻る

### 修正

- 右クリックメニューを、ウィンドウの高さに応じて平らな一覧とサブメニュー構成の間で自動的に切り替えるようにした（Issue #137）。前回の対応（PR #146）でサブメニューへ折りたたんだところ、階層に隠れて操作性が落ちるという実機確認の指摘を受けたもの。メニューを開いた瞬間のウィンドウの高さで、平らな一覧（PR #146 より前と同じ並び）が収まるかどうかを見積もり、収まるときはそのまま、収まらないときだけ「表示」「ウィンドウ」のサブメニューにまとめる。判定はメニューを開いた瞬間に確定させ、開いている間は変えない
- 詳細設定（設定ダイアログ）が低い解像度・小さいウィンドウで画面からはみ出し、OK / キャンセル / 適用のボタン列が押せなくなることがあったのを修正した（Issue #137）。ダイアログそのものを画面内に収め、タブの中身をスクロール領域に入れることで、ボタン列は常にスクロール領域の外（画面内）に残るようにした。ホットキー入力ダイアログも同様に画面内へ収める
- USB を抜き差ししたあと、映像は自動で戻るのに音声が戻らない問題を修正した。原因は「設定のデバイスで 3 回続けて失敗したら Windows の既定のデバイスで開き直す」動作で、切断の直後は必ず 3 回失敗する（1.5 秒）一方、USB の再列挙には 10 秒以上かかるため、**PC のマイクを入力として掴んだ状態が接続成功として確定し、以後は設定のデバイスを試さなくなっていた**。副作用として、切断のたびに PC のマイクの音がスピーカーへ流れていた。**設定に書かれたデバイスで開けない場合、別のデバイスへは倒さず、戻るまで再試行を続けるようにした**（間隔は映像と同じく最大 5 秒）。この変更は起動時にも及ぶ。設定のデバイスが見つからないときに黙って別のデバイスを開くのは、音が出ないことより分かりにくい誤動作のため。繋がっていないことは設定画面の「接続状態」タブと画面の通知で分かる。あわせて、映像が途絶から復帰した時点で音声にも再接続を要求するようにした（同じ USB 機器なので同時に戻る）
- 入力と出力でサンプリングレートやチャンネル数が異なると音が崩れる問題を修正した。これまではリングバッファの値をそのまま出力していたため、入力 48000Hz・出力 44100Hz のような組み合わせでは再生速度とピッチがずれ、チャンネル数が違う場合はさらに崩れていた。揃えられない組み合わせでは線形補間でサンプリングレートを変換し、チャンネル数はモノラルと多チャンネルの間でアップ／ダウンミックスする。揃えられる組み合わせでは従来どおり変換を挟まない。なお、入出力のハードウェアクロックのずれ（長時間再生で効いてくる）の補正は入っていない
- 設定画面でホットキーをクリアして適用したあと、再起動すると既定の F5 に戻ってしまう問題を修正した。これまでは「未設定」を設定ファイルに書き残せず、項目が欠けているだけの状態と区別できなかった
- 設定に項目を追加したあとも、既存の設定ファイルを読めるようにした。これまでは項目が 1 つ増えるだけで、設定が全項目初期化される、または解像度やフレームレートの既定値が失われる状態だった
- 設定ファイルの読み込みに失敗した場合、読めなかったファイルを `.bak` として同じフォルダに残すようにした。これまでは何も残さずに既定値へ戻していた
- デバイスに接続できないとき、再接続を試みている間に画面が止まらないようにした。これまでは再試行のために最大 3.2 秒ぶん画面全体が固まっていた
- 設定画面でオーディオ出力デバイスだけを変更したときに、その変更が反映されるようにした。これまでは切り替えても古い出力先で鳴り続け、他の設定を変えるまで反映されなかった
- ウィンドウアイコンと既定の効果音を実行ファイルに埋め込むようにした。これまでは作業ディレクトリが実行ファイルの場所と異なると、アイコンが赤い四角になり、スクリーンショットの効果音が鳴らなかった。設定で選んだ効果音の相対パスも実行ファイルの置き場所を基準に解決するようにした
- 音声の出力を「既定のデバイス」にしているとき、Windows のサウンド設定で既定を切り替えても追従しない問題を修正した。cpal は WASAPI の既定デバイス変更通知（`IMMNotificationClient`）を公開していないため、開いたあとのストリームは古いデバイスのまま残っていた。数秒おきに Windows 側の既定デバイス名を確認し、実際に開いている名前と食い違っていれば再接続するようにした。設定ダイアログの開閉に関係なく動く。明示的にデバイスを選んでいる向きは対象外（既定を追う話であって、設定のデバイスを Windows の既定へ倒す話ではない）。入力デバイスは従来どおり起動時に列挙した先頭のデバイスへ自動で書き換えて確定させており対象外（未設定のままにすると、環境によっては内蔵マイクの音がスピーカーへ流れる。#134 参照）
- 入出力のサンプルレートが異なる組み合わせで長時間再生すると、クロックのわずかな差が積み重なりリングバッファが徐々に溜まる（遅延が増える）か枯れる（音が途切れる）問題を修正した（#132）。出力コールバックがリングバッファの水位を書き、デバイスワーカーが数秒ごとに目標水位（バッファ容量の半分）とのずれを見てレート比を ±0.1% の範囲で微調整する。揃っている組み合わせ（変換が要らない場合）は対象外

## [1.0.6] - 2025-08-23

### 追加

- 終了時にウィンドウの位置とサイズを保存し、次回起動時に復元するようにした

## [1.0.5] - 2025-08-20

### 追加

- 設定画面にビデオのフォーマット・解像度・フレームレートの項目を追加した
- 接続中のデバイスから対応する設定値を取得し、選択肢として表示するようにした。フォーマットを選ぶと対応する解像度が、解像度を選ぶと対応するフレームレートが絞り込まれる
- 設定画面にオーディオのサンプリングレートとチャンネル数の項目を追加した

### 変更

- 設定画面の反映タイミングを整理した。設定値の保存は OK ボタン押下時のみ、適用は 適用 または OK ボタン押下時に行う
- デバイス一覧をキャッシュして表示するようにした

## [1.0.4] - 2025-08-11

最初の公開リリース。

### 追加

- キャプチャーデバイスの映像表示
- 音声のパススルー再生
- 音量調整（0〜200%）。マウスホイールとコンテキストメニューから操作できる
- アスペクト比の維持切り替え
- 最前面表示の切り替え
- フルスクリーン表示の切り替え（ダブルクリック）
- 映像部分のドラッグによるウィンドウ移動
- グローバルホットキーによるスクリーンショット保存（既定 F5）
- スクリーンショットの保存先指定と効果音の設定
- 設定の永続化（`%AppData%\capturecard_viewer`）

---

## 記入の方針

- **ユーザーから見える変更を書く。** 内部のリファクタリングは、挙動が変わらないなら書かない
- 分類は 追加 / 変更 / 非推奨 / 削除 / 修正 / セキュリティ を使う
- 1.0.4 から 1.0.6 の内容は、当時のコミットの差分から事後的に再構成したもの。実際のリリース時点の記録ではない
